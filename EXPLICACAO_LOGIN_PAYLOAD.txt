Redirecionamento pós-login (303 PRG)
→ Depois do login, o navegador é redirecionado para /home.
👉 Assim, o POST com email/senha não fica reaparecendo se o usuário der refresh.

Cookies e cabeçalhos de segurança
→ Cookies só acessíveis pelo servidor (HttpOnly) e, quando em HTTPS, com Secure e HSTS.
👉 Evita que scripts roubem cookies e adiciona proteção contra ataques comuns.

Nomes neutros nos campos
→ Em vez de nomes iguais às colunas (ex.: email/senha), usamos session_key e encrypted_session_key (e chaves neutras nos outros formulários: a_*, h_*, s_*).
👉 Quem abrir o DevTools não verá nomes de colunas ou tabelas do banco nem "email"/"senha" no Payload.

Senha ofuscada no cliente (Base64)
→ A senha é convertida para Base64 antes de ser enviada.
👉 No DevTools não aparece em texto puro (a segurança real vem do HTTPS).

Backend compatível
→ O servidor entende tanto o formato antigo quanto o novo e decodifica a senha se vier em Base64.
👉 Nada quebra para quem ainda usa o fluxo antigo.

Política de segurança de conteúdo (CSP)
→ Ajustada para permitir estilos/scripts do CDN sem quebrar o layout.
👉 Ajuda a evitar injeção de scripts maliciosos.

✅ Resumo bem simples:
- Pós-login redireciona e não reaparece no refresh.
- Cookies mais protegidos (HttpOnly, e Secure/HSTS em HTTPS).
- Campos sensíveis com nomes neutros; nada de nomes de colunas no Payload.
- Senha ofuscada em Base64 no envio.
- Backend aceita formato antigo e novo.
- CSP e cabeçalhos extras deixam a aplicação mais segura.
